[server]
# Allow large video files (up to 500 MB) for preview and download
maxUploadSize = 500
maxMessageSize = 500

# Enable static file serving from the project directory
enableStaticServing = true

[browser]
gatherUsageStats = false
