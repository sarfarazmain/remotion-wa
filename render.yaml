services:
  - type: web
    name: warp-dashboard
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PEXELS_API_KEY
        sync: false
    healthCheckPath: /_stcore/health
    autoDeploy: true
